{
  "name": "ai-tools-directory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node generate-pages-local.js && vite",
    "build": "node generate-pages-netlify.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
